# issue-writer Skill 设计方案 v2

> 将自然语言转化为高质量 GitHub Issue 的交互式 AgentSkills
> 版本: draft-0.2

---

## 一、核心理念

### 1.1 设计前提

**用户不是职业程序员。** 他们可能是设计师、产品经理、测试、运营、或者普通用户。他们的长处是知道「什么地方不对」「想要什么」，短处是不懂技术术语和 issue 的规范格式。

因此这个 skill 的核心价值不是「翻译」，而是 **引导式对话** — 像有一个有经验的开发者在帮他们梳理问题。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| **先问清楚，再写** | 信息不够就追问，不编造 |
| **用自然语言问，不用术语** | 问问题要让非程序员也能听懂 |
| **能读懂的不问** | 用户已经表达清楚的信息不重复问 |
| **通用化** | 任何项目、任何类型 issue 都能用 |
| **一次生成，直接可用** | 产出就能直接贴到 GitHub |

---

## 二、交互流程（核心）

```
触发技能

  │
  ▼
┌─────────────────────────────┐
│  第1步：确认项目             │
│                             │
│  如果用户没说给哪个项目：      │
│  → 询问「你是在说哪个项目？」  │
│  → 列选项或自由输入           │
│                             │
│  如果用户已经说了：直接跳过    │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│  第2步：理解 issue 类型       │
│                             │
│  从用户话语中自动推断类型      │
│  bug / feature / perf / UX  │
│  如果无法推断：简单问一句      │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│  第3步：信息提取与缺口分析     │
│                             │
│  从用户已有描述中提取已知信息   │
│  对照模板识别缺失的关键字段     │
│  列出需要追问的问题列表        │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│  第4步：逐轮追问（核心）       │
│                             │
│  每次问一个问题              │
│  用自然语言、不用术语         │
│  提供示例让用户好回答         │
│  用户回答后更新信息，继续追问  │
│  直到核心字段都补齐或用户说    │
│  「够了就先这样」             │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│  第5步：生成 issue            │
│                             │
│  输出完整 issue 正文          │
│  附带质量评分                │
│  标记仍在用的「推断」和缺口    │
│  问用户「可以直接用吗？」      │
└─────────────────────────────┘
```

### 2.1 追问策略详解

#### 追问原则

1. **每次只问一个问题** — 不要一次性甩 5 个问题给用户
2. **用自然语言问** — 不要说「请提供环境信息」，说「你是在什么浏览器上遇到的？」
3. **提供回答模板** — 让用户好接话
4. **根据已答动态调整** — 用户多说了就吸收，不问重复问题
5. **用户可以说「先这样」** — 标记缺口继续生成，不强制

#### 追问优先级

```
高优先级（必须问）：
  1. 项目（如果没说）
  2. 到底发生了什么 / 想要什么
  3. 怎么复现 / 使用场景

中优先级（建议问）：
  4. 环境（浏览器/OS/设备）
  5. 期望结果 vs 实际结果

低优先级（可跳过）：
  6. 日志/错误信息
  7. 截图
  8. 优先级/紧急程度
```

#### 不同类型的问题措辞示例

```
bug 类：
  ❌ 错误措辞：「请提供 Step to reproduce」
  ✅ 正确措辞：「你能说说怎么操作才会出现这个问题的吗？比如先点哪里再点哪里」

  ❌ 错误措辞：「请填写 Expected/Actual behavior」
  ✅ 正确措辞：「你本来期望点完按钮之后发生什么？但实际上发生了什么？」

feature 类：
  ❌ 错误措辞：「Please describe acceptance criteria」
  ✅ 正确措辞：「你希望这个功能做成什么样子？有没有参考过其他类似的产品？」

perf 类：
  ❌ 错误措辞：「Provide performance metrics」
  ✅ 正确措辞：「大概有多慢？比如说原来等 1 秒现在要等 10 秒这种程度？」
```

---

## 三、对话示例（完整流程）

> 💬 用户: 「注册页点按钮没反应」

```
Agent: 你说的是哪个项目呀？😊

用户: vibeweave

Agent: 好，让我确认一下 — 你遇到的是「注册页点按钮没反应」这个问题，是 bug 对吧？

用户: 对

Agent: 好的。你是在什么浏览器上遇到的？

用户: Chrome

Agent: 操作系统呢？Windows / Mac / 还是手机？

用户: Mac

Agent: 能说说你是怎么操作的才出现这个问题的吗？比如先打开什么页面，填了什么，然后点了哪里？

用户: 就打开注册页，填了用户名和密码，点提交按钮就没反应了

Agent: 你本来期望点了提交按钮之后发生什么？

用户: 应该跳转到确认页或者报个错吧

Agent: 好的，信息差不多了！还有什么要补充的吗？比如有没有报错信息或者截图？

用户: 没了，就这样吧

Agent:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ## 描述
  注册页面点击提交按钮后无响应，无法完成注册流程。

  ## 复现步骤
  1. 打开 vibeweave 注册页面
  2. 填写用户名和密码
  3. 点击提交按钮
  4. 页面没有任何反应，未跳转也未提示错误

  ## 期望行为
  点击提交后应触发注册流程，跳转至确认页面或给出明确的错误提示。

  ## 实际行为
  点击提交后页面无任何响应。

  ## 环境
  - 操作系统：macOS
  - 浏览器：Chrome
  - 项目：vibeweave

  ## 截图/录屏
  <!-- TODO: 请补充截图 -->

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ⭐⭐⭐☆☆ (3/5) — 环境信息和操作步骤完整，缺截图和错误日志
  可以直接用吗？有什么需要调整的？

用户: 可以了，帮我建个 issue 吧

Agent: （调用 github skill 创建 issue）
```

---

## 四、Skill 元数据与结构

```yaml
---
name: issue-writer
description: >
  Use when converting natural language descriptions into structured GitHub Issues
  through interactive conversation. Triggered by fuzzy bug reports, vague feature
  requests, unstructured feedback, or any chat message that should become a proper
  GitHub issue. Guides non-technical users step-by-step through issue creation.
  适用于将自然语言问题转化为结构化 GitHub Issue，通过对话引导非技术人员
  提供完整信息后再生成 issue。
user-invocable: true
---
```

```
issue-writer/
  SKILL.md                    # 核心 skill（< 500 行）
  references/
    templates.md              # issue 模板库
    questioning-guide.md      # 追问策略与措辞指南
    examples.md               # 完整对话示例（含追问版）
```

---

## 五、模板规范（通用化设计）

所有模板采用**统一结构骨架**，不同 issue 类型只是某些区块权重不同：

```
## 问题描述
{核心描述}

## 背景/上下文
{项目、功能、影响面}

## {类型相关区块}
{根据 bug / feature / perf 有不同的核心区块}

## 环境
{项目 / 浏览器 / OS / 设备}

## 补充信息
{截图、日志、参考链接}

## 原始描述
> _{用户最初的描述}_
```

### Bug Report

```
## 问题描述
## 复现步骤
## 期望行为
## 实际行为
## 环境
## 截图/日志
## 原始描述
```

### Feature Request

```
## 问题描述（想要什么）
## 使用场景（什么时候会用到）
## 期望效果（做出来什么样）
## 参考案例（有没有类似产品）
## 技术考量（提供给开发者参考）
## 原始描述
```

### Performance / UI-UX / General

同样骨架，核心区块替换即可。

---

## 六、质量评分（Footer）

每次生成附带评分，帮助用户判断是否能直接提交。

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Issue 质量：
  • 信息完整度: ⭐⭐⭐☆☆ (3/5)
  • 缺失项: 截图、错误日志
  • 建议: 补充截图后可提交
```

评分规则：

| 得分 | 条件 | 建议 |
|------|------|------|
| 5/5 | 所有字段完整，有数据有步骤 | 直接提交 |
| 3-4/5 | 核心信息完整，缺次要内容 | 补充后提交 |
| 1-2/5 | 核心信息不全，推断较多 | 建议补充更多信息 |
| 0/5 | 完全无法理解 | 继续追问 |

---

## 七、通用化适配

### 7.1 为什么是通用的？

- **不绑定任何项目** — 没有硬编码的仓库名
- **不绑定任何语言** — 中文/英文/双语输入都支持
- **不绑定任何 issue 平台** — 生成的是标准 markdown，GitHub / GitLab / Gitee / Jira 都能用
- **不绑定任何模板格式** — 输出的是清晰结构化 markdown，不是 GitHub YAML form

### 7.2 项目上下文感知

如果对话历史中已经有当前项目的信息（比如之前讨论过 vibeweave），自动使用已知信息：

- 项目名称
- 项目技术栈（帮助更精准地提问）
- 仓库地址（如果后续要发 issue 时用）

如果完全不知道，就通用化处理。

---

## 八、与 「code-jarvis」agent 的关系

```
触发方式：
  用户 /issue <描述>         → 直接在当前对话启动
  用户：「有个bug」「我想加个功能」  → 自然语言触发

执行者：issue-writer skill（由 code-jarvis agent 加载）

后续动作（可选）：
  用户说「帮我发 issue」
  → code-jarvis 调用 github skill 创建 issue
  → 返回 issue URL
```

skill 本身只负责生成内容，不调用 GitHub API。创建 issue 是 code-jarvis agent 的职责。

---

## 九、边界与限制

- **不编造信息** — 推断用 `[推断：]` 标记，缺失用 `<!-- TODO -->`
- **不一次问太多** — 每次一个问题，逐步完善
- **不强求填完** — 用户说「先这样」就生成
- **不提交到 GitHub** — 只生成内容，提交是 agent 的决策
- **不过度追问** — 关键字段补齐后，问一次「还有什么要补充吗」就停
