{
  "name": "kold",
  "version": "0.2.1",
  "description": "Cross-platform AI coding agent specialized in frontend design and UI implementation",
  "private": true,
  "scripts": {
    "lint": "npm run lint:html && npm run lint:css",
    "lint:html": "htmlhint examples/",
    "lint:css": "sh -c 'stylelint \"**/*.css\" || true'"
  },
  "devDependencies": {
    "htmlhint": "^1.1.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
